import BaseCollection from '@discordjs/collection';

/**
 * Structure for storing cache in the managers
 */
export default class Collection<K, V> extends BaseCollection<K, V> {
  // will add something here later
}
